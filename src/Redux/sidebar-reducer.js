let initialState = {
    firstFriend: [
        {img: '#', name: 'Elon'}
    ],
    secondFriend: [
        {img: '#', name: 'Mike'}
    ],
    thirdFriend: [
        {img: '#', name: 'Anton'}
    ],
};

const sidebarReducer = (state = initialState, action) => {
    return state;
};

export default sidebarReducer;